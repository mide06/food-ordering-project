// ─── Cart Authentication Utilities ──────────────────────────────
// Note: Authentication utilities are now handled by shared utils.js

// Initialize on page load
document.addEventListener('DOMContentLoaded', () => {
  // User authentication display handled by utils.js
  // Optionally, redirect to login if cart requires authentication
  // Uncomment below if you want to force login for cart.html
  // if (!isLoggedIn()) window.location.href = '/login files/login.html';
});
